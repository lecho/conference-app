<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.lecho.mobilization">

    <application
        android:name=".ConferenceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:installLocation="auto"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.TalkActivity">
        </activity>
        <activity android:name=".ui.SpeakerActivity">
        </activity>

        <service
            android:name=".async.ContentUpdateService"
            android:exported="false">
        </service>

        <activity
            android:name=".ui.MainTabbedActivity"
            android:label="@string/title_activity_main_tabbed"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>